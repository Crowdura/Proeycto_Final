export const Button = (props) => {
    return <button className="ButtonB" {...props} ></button>
}

export const ButtonForm = (props) => {
    return <button className="ButtonForm" {...props}></button>
} 